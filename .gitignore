## .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
*.cache

## Publish output
publish/
*.pubxml.user

## Installer build output
installer/out/

## Rider / VS / VS Code
.vs/
.idea/
*.DotSettings.user

## macOS
.DS_Store

## Claude Code local overrides (project CLAUDE.md and skills are tracked)
.claude/settings.local.json

# .NET build output for the macOS shell
mac/**/bin/
mac/**/obj/

# MSBuild SDK-resolver backups left behind when an IDE without the macos workload loads the Mac project
*.SdkResolver.*.proj.Backup.tmp

# The landing page carries the current MSI when it is copied to the website; the repo does not
website/downloads/*.msi

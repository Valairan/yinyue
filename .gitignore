## .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
*.cache

## Publish output
publish/
*.pubxml.user

## Installer build output
installer/out/

## Rider / VS / VS Code
.vs/
.idea/
*.DotSettings.user

## macOS
.DS_Store

## Claude Code local overrides (project CLAUDE.md and skills are tracked)
.claude/settings.local.json

# .NET build output for the macOS shell
mac/**/bin/
mac/**/obj/

# MSBuild SDK-resolver backups left behind when an IDE without the macos workload loads the Mac project
*.SdkResolver.*.proj.Backup.tmp

# The MSI is built on Windows and copied in when the site is assembled; it is not committed.
website/downloads/*.msi

# The DMG is committed, so the macOS download works from a clone. It is 85 MB against a 7 MB
# repository, and git keeps blobs forever — a later change of mind means rewriting history.
!website/downloads/*.dmg
installer/obj/

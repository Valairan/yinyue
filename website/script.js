// The page works with no JavaScript at all. The background loop is a plain GIF, so there is
// nothing to start, resume or fall back from; this file is kept so the script tag has a home
// if something ever needs one.
